apiVersion: v1
kind: Pod
metadata:
  name: flask
  labels:
    app: flask
spec:
  containers:
  - name: flask-env
    image: kunchalavikram/flask-env:latest
    ports:
    - containerPort: 5000
    env:
    - name: BACKGROUND_COLOR
      value: "blue"
    - name: TEXT_COLOR
      value: "white"
---
apiVersion: v1
kind: Service
metadata:
  name: flask
spec:
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 5000
    nodePort: 31000
    protocol: TCP
  selector:
    app: flask