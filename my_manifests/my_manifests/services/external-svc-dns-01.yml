apiVersion: v1
kind: Service
metadata:
  name: externalsvc-dns
spec:
  type: ExternalName
  externalName:  cat-fact.herokuapp.com


# kubectl run -i --tty --image nginx:alpine test-pod --restart=Never --rm -- sh
# curl externalsvc-dns/fact